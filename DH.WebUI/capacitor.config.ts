import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dicehubs.app',
  appName: 'DiceHubs',
  webDir: 'dist/dh.web-ui/browser'
};

export default config;
