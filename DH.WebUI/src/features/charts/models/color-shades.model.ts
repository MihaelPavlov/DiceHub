export interface ColorShades {
  default: string; // Fully opaque color
  half?: string; // Semi-transparent color
  quarter?: string; // More transparent color
  zero?: string; // Fully transparent color
}
